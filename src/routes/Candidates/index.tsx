import { Layout } from '../../components';

type Props = {};

const Candidates = ({}: Props): JSX.Element => {
  return (
    <Layout>
      <h2>Candidates</h2>
    </Layout>
  )
};

export { Candidates };
